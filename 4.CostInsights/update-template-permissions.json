{
    "AwsAccountId": "${AwsAccountId}",
    "TemplateId": "${TemplateId}",
    "GrantPermissions": [{
      "Principal": "arn:aws:iam::${TargetAccountId}:root",
      "Actions": [
        "quicksight:UpdateTemplatePermissions",
        "quicksight:DescribeTemplate"
      ]}
    ]
}
